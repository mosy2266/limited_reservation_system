# 프로젝트 진행 간 의사 결정 문서

## 목차

- [1. 현재 패키지 구조를 도메인 중심으로 구성한 이유](#1-현재-패키지-구조를-도메인-중심으로-구성한-이유)
- [2. Redis 기반 Rate Limiting을 도입한 이유](#2-redis-기반-rate-limiting을-도입한-이유)
- [3. TPS 급증에 대비한 시스템 붕괴 방지 구조](#3-tps-급증에-대비한-시스템-붕괴-방지-구조)
- [4. 결제 수단별 Strategy Pattern을 채택한 이유](#4-결제-수단별-strategy-pattern을-채택한-이유)
- [5. Redis 장애 시 Fallback 전략](#5-redis-장애-시-fallback-전략)
- [6. 결제 실패 케이스 대응 로직](#6-결제-실패-케이스-대응-로직)
- [7. 최종 재고 정합성 보장 방식](#7-최종-재고-정합성-보장-방식)
- [8. MSA 확장을 고려한 서버 간 정합성 보장 방식](#8-msa-확장을-고려한-서버-간-정합성-보장-방식)
- [9. Kafka를 도입한 이유](#9-kafka를-도입한-이유)
- [10. Transactional Outbox 패턴을 도입한 이유](#10-transactional-outbox-패턴을-도입한-이유)
- [11. 정합성 및 동시성 테스트 전략](#11-정합성-및-동시성-테스트-전략)

## 1. 현재 패키지 구조를 도메인 중심으로 구성한 이유

### 쟁점

- 제한 수량 예약 시스템의 패키지 구조를 어떻게 나눌 것인가?
- 선택지
  - 계층 중심 패키지: `controller`, `service`, `domain`, `repository`
  - 도메인 중심 패키지: `booking`, `payment`, `inventory`, `product` 등
  - 완전한 멀티 모듈 구조
- 핵심 판단 기준
  - 예약, 결제, 재고, 상품의 책임 경계가 명확한가?
  - 분산 환경에서 정합성 및 장애 대응 흐름을 추적하기 쉬운가?
  - 향후 MSA 또는 모듈 분리로 확장하기 쉬운가?
  - 현재 프로젝트 규모에 비해 구조 복잡도가 과하지 않은가?

### 문제 상황

- 이 시스템은 하나의 예약 확정 흐름에서 여러 도메인이 함께 동작한다.
  - 주문서 진입
  - 상품 판매 가능 여부 확인
  - Redis 기반 재고 선점
  - DB 기반 최종 재고 차감
  - 결제 승인
  - 예약 저장
  - 아웃박스 이벤트 저장 및 발행
- 계층 중심 패키지를 사용하면 관련 클래스가 여러 패키지에 흩어진다.
  - 예약 흐름 파악 시 `controller`, `service`, `repository`, `domain` 패키지를 계속 오가야 한다.
  - 특정 장애나 요구 사항 변경이 어느 도메인 책임인지 드러나기 어렵다.
  - 향후 서비스 분리 시 어떤 클래스를 함께 분리해야 하는지 판단하기 어렵다.
- 완전한 멀티 모듈 구조는 현재 단계에서는 비용이 크다.
  - Gradle 모듈 설정이 추가된다.
  - 모듈 간 의존성 방향을 별도로 관리해야 한다.
  - 테스트 구성과 공통 타입 분리 비용이 증가한다.
  - 아직 핵심 API와 정합성 흐름을 안정화하는 단계에서는 구조 비용이 과할 수 있다.

### 고려했던 대안들

#### 대안 1. 계층 중심 패키지

- 장점
  - 구조가 익숙하고 단순하다.
  - 클래스 역할을 이름만으로 구분하기 쉽다.
- 단점
  - 도메인 경계가 패키지 구조에 드러나지 않는다.
  - 예약, 결제, 재고, 아웃박스 흐름이 여러 계층 패키지에 흩어진다.
  - 변경 영향 범위를 도메인 단위로 파악하기 어렵다.
  - MSA 전환 시 분리 기준을 다시 정리해야 한다.
- 판단
  - 이 프로젝트의 복잡도는 계층보다 도메인 간 협력과 정합성에 있다.
  - 따라서 계층 중심 구조는 핵심 문제를 드러내는 데 적합하지 않다.

#### 대안 2. 완전한 멀티 모듈 구조

- 장점
  - 도메인 간 의존성 경계를 강하게 제한할 수 있다.
  - 향후 서비스 분리를 전제로 하면 가장 명확한 구조다.
- 단점
  - 현재 프로젝트 규모에 비해 설정 비용이 크다.
  - 모듈 간 테스트, 공통 타입, 빌드 설정 관리가 먼저 복잡해진다.
  - 핵심 예약 흐름 구현보다 구조 관리 비용이 커질 수 있다.
- 판단
  - 장기적으로 고려할 수 있지만, 현재 단계에서 바로 도입하기에는 과하다.
  - 우선 단일 모듈에서 도메인 경계를 명확히 한 뒤 필요 시 분리한다.

#### 대안 3. 단일 모듈 + 도메인 중심 패키지

- 장점
  - 현재 구현 비용을 낮게 유지할 수 있다.
  - 예약, 결제, 재고, 상품, 아웃박스의 책임 경계를 코드 구조에 드러낼 수 있다.
  - 장애 대응 시 확인해야 할 영역을 좁힐 수 있다.
  - 테스트를 도메인 책임 단위로 작성하기 쉽다.
  - 향후 `booking`, `payment`, `inventory`, `outbox`를 모듈 또는 서비스 단위로 분리하기 쉽다.
- 단점
  - 한 도메인 패키지 안에 여러 계층 클래스가 함께 위치한다.
  - 패키지 내부 클래스 수가 많아지면 가독성이 떨어질 수 있다.
- 판단
  - 현재 구현 단계에서는 구조 복잡도보다 도메인 책임 경계가 더 중요하다.
  - 단일 모듈의 단순함을 유지하면서도 향후 분리 가능성을 남길 수 있으므로 이 방식을 선택한다.

### 최종 판단

- 단일 Spring Boot 애플리케이션 안에서 도메인 중심 패키지 구조를 채택한다.
- 패키지 분리 기준은 기술 계층이 아니라 업무 책임이다.
- 이유
  - 제한 수량 예약 시스템의 핵심은 도메인별 책임 경계와 정합성 보장이다.
  - 단일 애플리케이션의 단순함을 유지하면서도 MSA 전환 가능성을 남길 수 있다.
  - 현재 규모에서는 멀티 모듈보다 패키지 경계만으로도 충분한 구조적 이점을 얻을 수 있다.
- 주요 패키지 책임
  - `checkout`: 주문서 진입 화면에 필요한 상품 및 사용자 결제 정보 조회
  - `booking`: 예약 생성, 멱등성 검증, 중복 예약 방지, 예약 확정 흐름 조합
  - `inventory`: Redis 기반 재고 선점, DB 기반 최종 재고 정합성 처리
  - `payment`: 신용카드, Y페이, Y포인트 결제 검증 및 결제 수단 확장 지점
  - `product`: 판매 대상 상품과 판매 가능 상태 관리
  - `outbox`: 예약 확정 이벤트 저장, 재시도, 발행 상태 관리
  - `messaging`: 외부 메시지 브로커 발행 추상화와 구현
  - `common`: 공통 응답, 예외, 에러 코드
- 계층별 책임 원칙
  - Controller: 요청과 응답 처리
  - Service: 유스케이스 흐름 조합
  - Domain: 핵심 정책과 검증
  - Repository: 영속성 처리
- 보완 기준
  - 특정 도메인 패키지가 커지면 하위 패키지를 추가한다.
  - 도메인 간 독립 배포 또는 강한 의존성 제한이 필요해지면 멀티 모듈로 분리한다.

## 2. Redis 기반 Rate Limiting을 도입한 이유

### 쟁점

- 프로모션 시작 직후 순간적으로 몰리는 예약 요청을 어떻게 제어할 것인가?
- 단순히 서버 부하를 줄이는 것이 아니라, 모든 사용자가 비슷한 조건에서 구매를 시도할 수 있도록 해야 한다.
- 핵심 판단 기준
  - 특정 사용자의 반복 요청이 구매 확률을 비정상적으로 높이지 않는가?
  - 2대 이상의 애플리케이션 서버에서도 동일한 기준으로 요청을 제한할 수 있는가?
  - DB 재고 차감, 결제 검증, 예약 저장 같은 핵심 트랜잭션으로 불필요한 요청이 과도하게 유입되지 않는가?
  - Redis 장애 시에도 최종 예약 및 재고 정합성을 유지할 수 있는가?

### 문제 상황

- 제한 수량 상품은 구매 가능 수량보다 구매 희망자가 많다.
- 같은 사용자가 짧은 시간에 요청을 반복하면 정상적으로 한 번 시도한 사용자보다 유리해질 수 있다.
- 00시 프로모션 시작 직후에는 1~5분 동안 500~1000TPS 수준의 순간 트래픽이 발생할 수 있다.
- Rate Limiting 없이 모든 요청을 예약 처리 흐름까지 전달하면 다음 문제가 생긴다.
  - DB 조건부 재고 차감 요청이 급격히 증가한다.
  - 멱등성 검증, 중복 예약 검증, 결제 검증까지 불필요하게 수행될 수 있다.
  - 반복 요청 사용자가 시스템 자원과 구매 기회를 더 많이 점유한다.
- 애플리케이션 서버가 2대 이상이면 서버별 로컬 상태만으로 사용자별 요청 빈도를 일관되게 판단하기 어렵다.

### 고려했던 대안들

#### 대안 1. 애플리케이션 로컬 메모리 기반 Rate Limiting

- 장점
  - 구현이 단순하다.
  - 외부 미들웨어 의존이 없다.
  - 메모리 접근이므로 응답이 빠르다.
- 단점
  - 서버 인스턴스마다 제한 상태가 분리된다.
  - 같은 사용자의 요청이 여러 서버로 분산되면 제한을 우회할 수 있다.
  - 서버 재시작 시 제한 상태가 사라진다.
- 판단
  - 단일 서버에서는 사용할 수 있지만, 2대 이상의 애플리케이션 서버 환경에서는 공정한 사용자별 제한을 보장하기 어렵다.

#### 대안 2. DB 기반 Rate Limiting

- 장점
  - 중앙 저장소를 사용하므로 서버 간 일관성을 확보하기 쉽다.
  - DB 트랜잭션과 제약 조건을 활용해 제한 기록을 명확히 남길 수 있다.
- 단점
  - 프로모션 시작 시점의 순간 트래픽을 DB가 직접 받는다.
  - 핵심 원장인 재고, 예약, 결제 테이블 처리와 부하가 겹칠 수 있다.
  - 짧은 TTL 기반의 진입 제어를 DB로 처리하면 쓰기 비용이 과하다.
- 판단
  - DB는 최종 정합성 원장으로 남겨야 한다.
  - 진입 제어는 DB보다 가벼운 계층에서 먼저 처리하는 편이 적합하다.

#### 대안 3. Redis 기반 사용자 단위 Rate Limiting

- 장점
  - 모든 애플리케이션 서버가 같은 Redis 키를 기준으로 요청을 제한할 수 있다.
  - `SET NX`와 TTL을 사용해 짧은 시간 창의 반복 요청을 빠르게 차단할 수 있다.
  - DB에 도달하는 불필요한 요청을 줄여 핵심 예약 트랜잭션의 부담을 낮출 수 있다.
  - 상품과 사용자 단위로 제한 키를 구성하면 특정 상품에 대한 사용자별 반복 진입을 제어할 수 있다.
- 단점
  - Redis 장애 시 Rate Limiting 기능이 약해질 수 있다.
  - Rate Limiting은 구매 순서나 구매 성공을 보장하지 않는다.
  - 네트워크 지연이나 Redis timeout이 발생하면 진입 제어 판단이 불안정해질 수 있다.
- 판단
  - 분산 환경에서 사용자별 요청 기회를 제한하기에 가장 적합하다.
  - 단, Redis는 진입 제어 계층으로만 사용하고 최종 정합성은 DB에서 보장해야 한다.

### 최종 판단

- Redis 기반 사용자 단위 Rate Limiting을 도입한다.
- 현재 구현은 `BookingTrafficGuard`에서 `productId + userId` 기준의 rate key를 사용한다.
- 동일 사용자의 동일 상품 예약 진입은 1초 TTL로 제한한다.
- Rate Limiting의 목적
  - 반복 요청으로 인한 기회 독점 완화
  - DB 재고 차감, 결제 검증, 예약 저장으로 유입되는 불필요한 요청 감소
  - 여러 애플리케이션 서버에서 동일한 사용자별 제한 기준 적용
- Rate Limiting의 목적이 아닌 것
  - 구매 성공 보장
  - 요청 순서 보장
  - 대기열 제공
- 이 결정에서의 공정성 의미
  - 모든 사용자가 반드시 동일한 결과를 얻는다는 의미가 아니다.
  - 동일 사용자가 짧은 시간에 반복 요청을 보내 구매 확률을 비정상적으로 높이는 상황을 줄인다는 의미다.
- Redis 장애 시 처리 기준
  - Redis 장애 시 Rate Limiting 품질은 낮아질 수 있다.
  - 상세 fallback 기준은 `5. Redis 장애 시 Fallback 전략`을 따른다.

## 3. TPS 급증에 대비한 시스템 붕괴 방지 구조

### 쟁점

- 프로모션 시작 직후 500~1000TPS 수준의 순간 트래픽을 어떻게 흡수할 것인가?
- 단순히 모든 요청을 끝까지 처리하는 것이 아니라, 핵심 원장인 DB와 예약 트랜잭션을 보호해야 한다.
- 핵심 판단 기준
  - Checkout 조회 트래픽이 DB로 직접 집중되지 않는가?
  - Booking 요청이 재고 차감, 결제 검증, 예약 저장까지 무제한으로 진입하지 않는가?
  - DB lock 경합과 connection 고갈을 줄일 수 있는가?
  - Kafka 장애가 예약 성공 흐름을 직접 실패시키지 않는가?
  - Redis 또는 Kafka 장애 시에도 최종 예약 및 재고 정합성을 유지할 수 있는가?

### 문제 상황

- 평시에는 약 50TPS지만, 프로모션 시작 직후부터 5분 동안 약 500~1000TPS가 예상된다.
- Checkout 조회와 Booking 요청이 동시에 몰리면 다음 작업이 한꺼번에 증가한다.
  - 상품 조회
  - 재고 확인 및 차감
  - 멱등성 검증
  - 중복 예약 검증
  - 결제 검증
  - 예약 및 결제 저장
  - 예약 확정 이벤트 발행
- 모든 요청을 DB 중심으로 동기 처리하면 다음 위험이 커진다.
  - DB connection 고갈
  - 재고 row lock 경합
  - transaction timeout
  - 중복 결제 또는 중복 예약 시도 증가
  - 메시지 브로커 장애 시 API 응답 지연 또는 실패 전파
- 인프라 증설이 제한적인 상황이므로 서버를 늘리는 방식만으로는 충분하지 않다.
- Redis, Kafka 같은 미들웨어가 장애를 일으켜도 최종 예약 성공 여부와 재고 정합성은 DB 기준으로 안전하게 유지되어야 한다.

### 고려했던 대안들

#### 대안 1. 모든 요청을 DB 중심 동기 처리로 수용

- 장점
  - 구조가 단순하다.
  - 데이터 판단 지점이 DB로 모인다.
  - Redis, Kafka 같은 미들웨어 의존을 줄일 수 있다.
- 단점
  - 순간 트래픽이 DB 원장에 직접 집중된다.
  - 재고 row lock 경합이 커진다.
  - DB connection과 transaction timeout 위험이 커진다.
  - 조회 요청과 예약 요청이 같은 DB 자원을 두고 경쟁한다.
- 판단
  - 제한 수량 예약 시스템에서 DB는 최종 정합성 원장으로 보호되어야 한다.
  - 모든 요청을 DB로 직접 보내는 구조는 시스템 붕괴 위험을 낮추지 못한다.

#### 대안 2. 서버 증설 중심 대응

- 장점
  - 애플리케이션 서버의 요청 처리량을 빠르게 늘릴 수 있다.
  - 애플리케이션 CPU, thread, connection 처리 여유를 확보할 수 있다.
- 단점
  - DB, Redis, Kafka 같은 공유 자원 병목은 그대로 남는다.
  - 서버 수가 늘수록 DB로 향하는 동시 요청 수도 함께 증가할 수 있다.
  - 인프라 증설이 제한적인 전제와 맞지 않는다.
- 판단
  - 서버 증설은 보조 수단이 될 수 있지만, 핵심 병목과 정합성 문제를 해결하지 못한다.

#### 대안 3. 다단계 완충 구조

- 장점
  - Checkout 조회 부하는 Redis cache로 먼저 흡수할 수 있다.
  - Booking 진입은 Redis 기반 rate limit, 멱등성 처리, 재고 선점으로 줄일 수 있다.
  - DB는 최종 재고 차감과 예약 저장 같은 정합성 판단에 집중할 수 있다.
  - 예약 이후 이벤트 발행은 Outbox로 분리해 Kafka 장애가 API 성공 여부를 직접 흔들지 않게 할 수 있다.
- 단점
  - Redis cache, Redis 재고, DB 원장의 역할을 명확히 구분해야 한다.
  - Redis 장애 시 완충 기능이 약해질 수 있다.
  - Outbox 이벤트 재시도와 중복 발행 가능성을 고려해야 한다.
- 판단
  - 현재 요구 사항과 구현 구조에 가장 적합하다.
  - 빠른 계층에서 요청을 줄이고, DB는 최종 정합성 원장으로 유지하는 방식이 시스템 붕괴 방지에 유리하다.

### 최종 판단

- TPS 급증에 대비해 다단계 완충 구조를 채택한다.
- 완충 계층은 역할별로 분리한다.
  - Checkout 조회 부하는 Redis cache + Cache warming으로 줄인다.
  - Booking 진입량은 Redis 기반 rate limit, 멱등성 보조 처리, 재고 선점으로 줄인다.
  - DB는 반드시 필요한 최종 정합성 판단에 집중시킨다.
  - 예약 이후 이벤트 전달은 Kafka와 Outbox로 비동기화한다.
- 세부 정책은 각 결정 기록을 따른다.
  - Redis 장애 fallback: `5. Redis 장애 시 Fallback 전략`
  - 최종 재고 확정: `7. 최종 재고 정합성 보장 방식`
  - Kafka 도입 이유: `9. Kafka를 도입한 이유`
  - Outbox 재시도: `10. Transactional Outbox 패턴을 도입한 이유`
- 최종 원칙
  - Redis는 빠른 완충 계층이다.
  - MySQL은 최종 정합성 원장이다.
  - Kafka는 예약 이후 비동기 이벤트 전달 수단이다.
  - 순간 트래픽은 앞단에서 줄이고, 반드시 필요한 요청만 DB 트랜잭션까지 도달하게 한다.

## 4. 결제 수단별 Strategy Pattern을 채택한 이유

### 쟁점

- 새로운 결제 수단이 추가될 때 Booking API의 비즈니스 로직 수정을 어떻게 최소화할 것인가?
- 결제 수단마다 승인 방식, 실패 사유, 외부 연동 방식이 달라질 수 있다.
- 핵심 판단 기준
  - Booking API가 결제 수단별 세부 정책을 알지 않아도 되는가?
  - 결제 수단 추가 시 기존 예약 흐름의 변경 범위를 최소화할 수 있는가?
  - 신용카드, Y페이, Y포인트, 복합 결제 규칙을 명확히 분리할 수 있는가?
  - 결제 실패 사유를 공통 예외 응답 구조로 변환할 수 있는가?

### 문제 상황

- Booking API는 예약 확정 흐름에서 결제를 반드시 수행해야 한다.
- 결제 수단은 현재 다음을 지원한다.
  - 신용카드
  - Y페이
  - Y포인트
  - 신용카드 + 포인트
  - Y페이 + 포인트
- 신용카드와 Y페이는 함께 사용할 수 없다.
- 결제 수단별로 실패 조건과 향후 외부 연동 방식이 다르다.
  - 신용카드: 카드 한도 초과
  - Y페이: 잔액 부족
  - Y포인트: 포인트 부족
- 결제 수단 분기를 BookingService에 직접 작성하면 다음 문제가 생긴다.
  - 결제 수단이 추가될 때마다 예약 유스케이스 코드가 수정된다.
  - 예약 생성, 재고 차감, 결제 검증 책임이 한 클래스에 섞인다.
  - 결제 수단별 테스트와 예약 흐름 테스트가 강하게 결합된다.
  - 실제 PG 연동이 추가될 때 Booking API의 변경 위험이 커진다.

### 고려했던 대안들

#### 대안 1. BookingService에서 결제 수단별 분기 처리

- 장점
  - 초기 구현이 단순하다.
  - 결제 흐름을 예약 흐름 안에서 한 번에 읽을 수 있다.
- 단점
  - 결제 수단이 추가될 때마다 BookingService를 수정해야 한다.
  - 예약 유스케이스가 결제 수단별 상세 정책을 알게 된다.
  - 결제 실패 사유가 늘어날수록 예약 흐름의 복잡도가 증가한다.
  - 결제 도메인 변경이 예약 도메인 변경으로 전파된다.
- 판단
  - BookingService는 예약 유스케이스 흐름만 조합해야 한다.
  - 결제 수단별 상세 정책을 BookingService에 두는 방식은 책임 분리에 맞지 않는다.

#### 대안 2. PaymentService 내부 switch 분기 처리

- 장점
  - BookingService에서 결제 수단별 분기를 제거할 수 있다.
  - 결제 관련 코드를 PaymentService로 모을 수 있다.
- 단점
  - 결제 수단이 추가될 때마다 PaymentService의 switch 문을 수정해야 한다.
  - 결제 수단별 정책이 한 클래스에 누적된다.
  - PG 연동 방식이 달라질수록 PaymentService가 비대해진다.
  - Open-Closed Principle을 지키기 어렵다.
- 판단
  - BookingService의 결합도는 낮출 수 있지만, 결제 도메인 내부 확장성은 부족하다.

#### 대안 3. 결제 수단별 Strategy Pattern

- 장점
  - 결제 수단별 승인 정책을 `PaymentProcessor` 구현체로 분리할 수 있다.
  - `PaymentService`는 지원 가능한 processor를 찾아 위임하는 역할만 담당한다.
  - 새 결제 수단 추가 시 기존 BookingService 수정 범위를 줄일 수 있다.
  - 결제 수단별 테스트를 독립적으로 작성하기 쉽다.
  - 실제 PG 연동 구현체가 추가되어도 예약 유스케이스 흐름을 유지할 수 있다.
- 단점
  - 결제 수단이 적을 때는 클래스 수가 늘어난다.
  - 공통 검증과 수단별 검증의 책임 경계를 명확히 정해야 한다.
  - 복합 결제 규칙은 별도 조합 검증이 필요하다.
- 판단
  - 결제 수단 확장 가능성이 명시된 요구 사항에 가장 적합하다.
  - Booking API의 변경을 최소화하면서 결제 도메인 내부에서 확장할 수 있다.

### 최종 판단

- 결제 수단별 Strategy Pattern을 채택한다.
- 결제 수단별 승인 처리는 `PaymentProcessor` 구현체로 분리한다.
  - `CardPaymentProcessor`: 신용카드 결제 처리
  - `PayPaymentProcessor`: Y페이 결제 처리
  - `PointPaymentProcessor`: Y포인트 결제 처리
- `PaymentService`는 다음 책임만 담당한다.
  - `PaymentMethod`를 지원하는 `PaymentProcessor` 탐색
  - 선택된 processor에 결제 요청 위임
  - 결제 실패 결과를 공통 `BusinessException`으로 변환
  - 복합 결제 시 포인트 결제 processor 추가 호출
  - 결제 조합 검증
- `BookingService`는 결제 수단별 구현체를 직접 알지 않는다.
- `BookingService`는 다음 수준의 결제 흐름만 조합한다.
  - 결제 금액과 포인트 금액 합계 검증
  - 결제 조합 검증 요청
  - 결제 승인 요청
- 향후 새 결제 수단이 추가될 때의 기본 변경 범위
  - `PaymentMethod`에 새 수단 추가
  - 새 `PaymentProcessor` 구현체 추가
  - 필요한 경우 `PaymentService.validateCombination`에 조합 규칙 추가
  - 해당 결제 수단 단위 테스트 추가
- 최종 원칙
  - Booking API는 예약 유스케이스 흐름만 조합한다.
  - 결제 수단별 세부 정책은 payment 도메인 내부에 둔다.
  - 결제 수단 추가가 예약, 재고, 아웃박스 흐름의 변경으로 번지지 않게 한다.

## 5. Redis 장애 시 Fallback 전략

### 쟁점

- Redis 장애 시 기능별로 어떤 방식의 fallback을 적용할 것인가?
- Redis는 다음 기능에 사용된다.
  - Checkout 상품 캐시
  - 사용자 단위 Rate Limiting
  - 멱등성 키 사전 점유
  - processing lock
  - 재고 선점
  - 실패 시 Redis 재고 복구
- 핵심 판단 기준
  - 어떤 기능은 fallback하고, 어떤 기능은 일시적으로 포기할 것인가?
  - Redis가 없어도 초과 판매를 막을 수 있는가?
  - Redis가 없어도 중복 예약을 막을 수 있는가?
  - Redis 장애가 정상 구매 가능한 사용자의 예약을 모두 차단하지 않는가?
  - Redis 장애 중 증가하는 DB 부하를 감수할 수 있는 fallback 기준이 명확한가?

### 문제 상황

- Redis는 빠른 조회, 진입 제어, 재고 선점을 위한 완충 계층이다.
- Redis는 최종 예약 성공 여부를 결정하는 원장이 아니다.
- Redis 장애 시 다음 기능은 약해질 수 있다.
  - Checkout cache hit
  - 사용자별 반복 요청 제한
  - Redis 기반 멱등성 사전 점유
  - Redis processing lock
  - Redis 재고 선점
  - Redis 재고 복구
- Redis 장애를 API 실패로 바로 전파하면 다음 문제가 생긴다.
  - Redis가 예약 시스템 전체의 단일 실패 지점이 된다.
  - DB 기준으로는 예약 가능한 상황에서도 사용자가 예약을 시도할 수 없다.
  - 조회 캐시 장애가 상품 조회 API 실패로 확대된다.
- 반대로 Redis 장애 시 모든 요청을 DB로 넘기면 다음 문제가 생긴다.
  - DB 부하가 증가한다.
  - Rate Limiting 효과가 사라져 반복 요청이 DB까지 도달한다.
  - 빠른 sold out 차단 효과가 약해진다.
- 따라서 Redis 장애 시에는 성능과 진입 제어 일부를 포기하더라도, DB 기준의 최종 정합성을 유지하는 전략이 필요하다.

### 고려했던 대안들

#### 대안 1. Redis 의존 기능 실패 시 API 실패로 전파

- 장점
  - DB fallback으로 인한 부하 증가를 막을 수 있다.
  - Redis 장애 상황을 빠르게 감지하고 실패 응답으로 전파할 수 있다.
  - Redis와 DB 상태 불일치 가능성을 줄일 수 있다.
- 단점
  - Redis가 단일 실패 지점이 된다.
  - Checkout cache 장애가 상품 조회 실패로 확대된다.
  - Redis 재고 선점 장애가 예약 불가로 확대된다.
  - DB 기준으로는 재고가 있어도 정상 사용자가 예약할 수 없다.
- 판단
  - Redis를 최종 원장이 아닌 완충 계층으로 사용하는 설계와 맞지 않는다.
  - 안정적인 예약 동작 보장 요구에도 맞지 않는다.

#### 대안 2. Redis 의존 기능을 모두 DB로 fallback

- 장점
  - Redis 장애 중에도 조회와 예약 흐름을 계속 유지할 수 있다.
  - 최종 판단을 DB로 일원화할 수 있다.
  - 정상 구매 가능한 사용자를 Redis 장애만으로 차단하지 않는다.
- 단점
  - 순간 트래픽이 DB로 직접 몰릴 수 있다.
  - Rate Limiting과 Redis 재고 선점 효과가 사라진다.
  - DB connection, lock 경합, timeout 위험이 커진다.
- 판단
  - 가용성 측면에서는 유리하지만 DB 보호 측면에서는 위험하다.
  - DB 조건부 업데이트, unique 제약, 멱등성 조회 같은 최종 정합성 장치가 반드시 전제되어야 한다.

#### 대안 3. 기능별 선택적 fallback

- 장점
  - Redis 장애가 발생해도 Checkout은 MySQL 조회로 fallback할 수 있다.
  - Redis 진입 제어가 실패해도 Booking은 DB 정합성 검증으로 진행할 수 있다.
  - Redis 재고 선점이 실패해도 DB 조건부 업데이트로 최종 재고를 판단할 수 있다.
  - Redis는 빠른 완충 계층으로 두고 MySQL을 최종 정합성 원장으로 유지할 수 있다.
- 단점
  - Redis 장애 중에는 성능이 저하될 수 있다.
  - DB 부하가 증가한다.
  - 사용자별 Rate Limiting과 빠른 sold out 차단 효과가 약해진다.
  - Redis 재고와 DB 재고의 재동기화 기준이 필요하다.
- 판단
  - 현재 요구 사항인 안정적인 동작과 엄격한 재고 정합성에 가장 적합하다.
  - Redis 장애 시 degraded mode로 동작하되, 최종 예약/재고 정합성은 DB 기준으로 유지한다.

### 최종 판단

- Redis 장애 시 기능별 선택적 fallback을 채택한다.
- Checkout cache fallback
  - `CheckoutProductCache.get()`은 Redis 장애 또는 역직렬화 실패 시 `Optional.empty()`를 반환한다.
  - `CheckoutService`는 cache miss와 동일하게 MySQL에서 상품을 조회한다.
  - Checkout cache 저장 실패는 조회 API 실패로 전파하지 않는다.
- Booking traffic guard fallback
  - `BookingTrafficGuard.tryEnter()`는 Redis `DataAccessException` 발생 시 `Optional.empty()`를 반환한다.
  - Rate Limiting, Redis 멱등성 사전 점유, processing lock은 적용되지 않을 수 있다.
  - 예약 흐름은 DB 기준 검증으로 계속 진행한다.
- Redis 재고 선점 fallback
  - `RedisInventoryStockGate.reserve()`는 Redis 장애 또는 재고 키 누락 시 `Optional.empty()`를 반환한다.
  - Redis 재고 선점이 없어도 예약 흐름은 DB 재고 차감 단계로 진행한다.
  - 최종 판매 확정은 `InventoryRepository.increaseSoldQuantityIfAvailable`의 DB 조건부 업데이트로 판단한다.
- Redis 재고 복구 실패 처리
  - 결제 실패나 DB 저장 실패 후 Redis 재고 복구가 실패해도 예약 트랜잭션 결과를 바꾸지 않는다.
  - DB가 최종 원장이며 Redis 재고는 별도 동기화 대상으로 본다.
- 최종 정합성 보장 기준
  - 초과 판매 방지: DB 조건부 재고 차감
  - 중복 예약 방지: 예약 unique 제약
  - 멱등성 결과 보장: DB에 저장된 idempotency key와 기존 예약 조회
- 최종 원칙
  - Redis 장애 중에는 성능과 진입 제어 품질 저하를 허용한다.
  - Redis 장애를 예약 시스템 전체 장애로 확대하지 않는다.
  - Redis는 보조 계층이고, MySQL은 최종 정합성 원장이다.

## 6. 결제 실패 케이스 대응 로직

### 쟁점

- 카드 한도 초과, 포인트 부족, 페이 잔액 부족 등 결제 실패를 예약 흐름에서 어떻게 처리할 것인가?
- 결제 실패 시 다음 조건을 모두 만족해야 한다.
  - 예약을 확정하지 않는다.
  - DB 재고 차감을 롤백한다.
  - Redis 재고 선점이 있었다면 복구한다.
  - 예약 확정 이벤트를 발행하지 않는다.
  - 사용자에게 명확한 실패 사유를 반환한다.
- 핵심 판단 기준
  - 결제 실패가 재고 누수로 이어지지 않는가?
  - 결제 실패가 잘못된 예약 확정으로 이어지지 않는가?
  - 결제 실패 사유를 공통 응답 구조로 전달할 수 있는가?
  - 실패 처리 로직이 BookingService에 과도하게 분산되지 않는가?

### 문제 상황

- Booking 흐름은 예약 확정을 위해 다음 작업을 순서대로 수행한다.
  - Redis 재고 선점
  - DB 조건부 재고 차감
  - 결제 검증 및 승인
  - 예약 저장
  - 결제 승인 내역 저장
  - 예약 확정 outbox 이벤트 저장
- 결제는 DB 재고 차감 이후 수행된다.
- 결제가 실패했는데 예약을 저장하면 실제 결제되지 않은 예약이 확정될 수 있다.
- 결제가 실패했는데 outbox 이벤트를 저장하면 외부 시스템에 잘못된 예약 확정 이벤트가 전달될 수 있다.
- Redis 재고 선점 이후 결제가 실패하면 Redis 재고를 복구하지 않을 경우 재고가 누수될 수 있다.
- DB 재고 차감은 같은 트랜잭션 안에서 롤백되어야 한다.
- 결제 실패 사유는 결제 수단별로 다르다.
  - 신용카드: 카드 한도 초과
  - Y페이: 잔액 부족
  - Y포인트: 포인트 부족
  - 기타 결제 실패

### 고려했던 대안들

#### 대안 1. 결제 실패도 예약/결제 실패 이력으로 DB에 저장

- 장점
  - 실패 이력을 추적하기 쉽다.
  - 고객 문의나 운영 분석에 활용할 수 있다.
  - 결제 재시도 정책을 붙이기 쉽다.
- 단점
  - 실패 예약 상태가 추가된다.
  - 실패 예약의 만료, 취소, 재시도 정책이 필요하다.
  - 실패 상태와 확정 상태를 구분하는 추가 검증이 필요하다.
  - 현재 필수 API 범위보다 상태 관리 복잡도가 커진다.
- 판단
  - 현재 단계에서는 재고 정합성과 잘못된 예약 확정 방지가 더 중요하다.
  - 실패 이력 저장은 추후 감사, 운영, 재시도 요구가 생기면 별도 결정으로 다룬다.

#### 대안 2. 결제 실패 시 예외 없이 실패 응답 객체로 분기

- 장점
  - 예외를 사용하지 않고 흐름을 명시적으로 표현할 수 있다.
  - 결제 실패와 결제 성공을 반환 타입으로 구분할 수 있다.
- 단점
  - BookingService가 실패 응답 분기, DB 롤백, Redis 재고 복구, outbox 미발행을 모두 직접 관리해야 한다.
  - 실패 처리 분기가 예약 생성 흐름 안에 계속 누적된다.
  - 재고 차감 이후 실패한 경우 트랜잭션 롤백 의도가 코드상 덜 명확해질 수 있다.
- 판단
  - 결제 실패는 예약 확정 불가 상태다.
  - 트랜잭션 롤백과 흐름 중단이 필요하므로 예외 기반 처리가 더 단순하다.

#### 대안 3. 결제 실패를 비즈니스 예외로 변환하고 예약 트랜잭션 롤백

- 장점
  - 결제 실패 즉시 예약 확정 흐름을 중단할 수 있다.
  - 같은 트랜잭션 안의 DB 재고 차감을 롤백할 수 있다.
  - 예약 저장, 결제 승인 저장, outbox 이벤트 저장을 수행하지 않게 할 수 있다.
  - `PaymentFailureReason`을 공통 `ErrorCode`로 변환해 표준 오류 응답을 유지할 수 있다.
  - Redis 재고 선점은 `finally`에서 복구할 수 있다.
- 단점
  - 현재 구조에서는 실패 결제 이력이 DB에 남지 않는다.
  - 외부 PG 연동 후에는 승인 취소, 망취소, 타임아웃 보상 처리 같은 추가 설계가 필요할 수 있다.
- 판단
  - 현재 필수 API 범위에서는 가장 단순하고 안전하다.
  - 결제 실패 시 예약 미확정, 재고 복구, 명확한 실패 응답이라는 목표에 가장 잘 맞는다.

### 최종 판단

- 결제 실패는 `PaymentService`에서 `BusinessException`으로 변환한다.
- 실패 사유는 `PaymentFailureReason`에서 공통 `ErrorCode`로 매핑한다.
  - `CARD_LIMIT_EXCEEDED`
  - `POINT_NOT_ENOUGH`
  - `PAY_BALANCE_NOT_ENOUGH`
  - `PAYMENT_FAILED`
- `BookingService`는 결제 실패 예외가 발생하면 이후 확정 처리를 수행하지 않는다.
  - 예약 저장을 수행하지 않는다.
  - 결제 승인 내역을 저장하지 않는다.
  - 예약 확정 outbox 이벤트를 저장하지 않는다.
- DB 처리 기준
  - 결제 실패 시 Booking 트랜잭션은 롤백된다.
  - 같은 트랜잭션 안에서 수행된 DB 재고 차감도 롤백된다.
- Redis 재고 처리 기준
  - Redis 재고 선점이 있었다면 `createBooking()`의 `finally` 블록에서 `stockGate.restore()`를 호출한다.
  - Redis 복구 실패는 별도 Redis 장애 fallback 기준을 따른다.
- 응답 처리 기준
  - `GlobalExceptionHandler`가 `BusinessException`을 공통 `ApiResponse` 오류 응답으로 변환한다.
  - 결제 실패 계열 오류는 422 응답으로 반환한다.
- 현재 범위에서 하지 않는 것
  - 실패 결제 이력을 별도 DB row로 저장하지 않는다.
  - 결제 재시도, 만료, 취소 상태 관리는 도입하지 않는다.
  - 실제 PG 연동에 필요한 보상 트랜잭션은 이번 범위에 포함하지 않는다.
- 최종 원칙
  - 결제 실패는 예약 실패다.
  - 결제되지 않은 예약은 확정하지 않는다.
  - 결제 실패는 재고 누수나 예약 확정 이벤트 발행으로 이어지지 않아야 한다.

## 7. 최종 재고 정합성 보장 방식

### 쟁점

- 분산 환경에서 동시에 예약 요청이 들어올 때 최종 재고 정합성을 어떻게 보장할 것인가?
- Redis 재고 선점, 애플리케이션 로직, DB 조건부 업데이트 중 무엇을 최종 판매 확정 기준으로 삼을지 결정해야 한다.
- 핵심 판단 기준
  - 2대 이상의 애플리케이션 서버에서도 초과 판매를 막을 수 있는가?
  - Redis 장애 또는 Redis 재고 불일치가 발생해도 최종 재고가 안전한가?
  - 결제 실패나 예약 저장 실패 시 재고 차감이 롤백되는가?
  - 중복 예약과 멱등성 중복 요청도 최종적으로 방어할 수 있는가?

### 문제 상황

- 제한 수량 상품은 동시에 여러 사용자가 예약을 시도한다.
- 재고 수량보다 많은 요청이 짧은 시간에 들어오면 초과 판매 위험이 발생한다.
- Redis 재고 선점은 빠른 사전 차단에는 유리하지만 다음 상황이 발생할 수 있다.
  - Redis 장애
  - Redis 재고 키 누락
  - Redis 재고 복구 실패
  - Redis 재고와 DB 재고 불일치
- 애플리케이션에서 재고를 조회한 뒤 차감하면 조회 시점과 저장 시점 사이에 경쟁 조건이 생길 수 있다.
- 결제 실패, 중복 예약 실패, DB 저장 실패가 발생하면 이미 선점하거나 차감한 재고를 되돌려야 한다.
- 같은 사용자의 중복 예약과 같은 멱등성 키의 중복 요청은 재고 차감과 별개로 최종 방어 장치가 필요하다.

### 고려했던 대안들

#### 대안 1. Redis 재고 값을 최종 기준으로 사용

- 장점
  - 응답이 빠르다.
  - DB 재고 차감 요청을 크게 줄일 수 있다.
  - 순간 트래픽이 몰릴 때 빠르게 sold out을 판단할 수 있다.
- 단점
  - Redis 장애 시 최종 판매 판단을 할 수 없다.
  - Redis 재고 복구 실패 시 실제 DB 재고와 불일치할 수 있다.
  - Redis는 예약, 결제, outbox 저장과 같은 DB 트랜잭션에 함께 묶이지 않는다.
  - 영속 원장으로 사용하기에는 복구와 감사 기준이 약하다.
- 판단
  - Redis는 빠른 완충 계층으로 사용한다.
  - 최종 재고 정합성 기준으로는 사용하지 않는다.

#### 대안 2. 애플리케이션에서 재고 조회 후 엔티티 메서드로 차감

- 장점
  - 도메인 메서드로 재고 정책을 표현하기 쉽다.
  - `Inventory.sell()` 같은 메서드로 비즈니스 의미가 명확해진다.
- 단점
  - 조회 시점과 저장 시점 사이에 경쟁 조건이 생길 수 있다.
  - 동시 요청에서 같은 재고 상태를 보고 모두 차감하려는 문제가 발생할 수 있다.
  - 별도 pessimistic lock 또는 optimistic lock 재시도 전략이 필요하다.
- 판단
  - 도메인 표현력은 좋지만, 초과 판매 방지를 단순하고 명확하게 보장하기에는 부족하다.
  - 현재 요구에는 DB 단일 조건부 업데이트가 더 적합하다.

#### 대안 3. DB row lock 기반 조회 후 차감

- 장점
  - 특정 상품 재고 row를 명시적으로 잠그고 검증할 수 있다.
  - 트랜잭션 안에서 재고 상태를 확인한 뒤 차감할 수 있다.
- 단점
  - 조회와 갱신이 분리된다.
  - 트래픽 급증 시 lock 점유 시간이 길어질 수 있다.
  - 결제 검증 같은 외부 처리와 함께 사용하면 lock 보유 시간이 길어질 위험이 있다.
- 판단
  - 사용할 수 있는 방식이지만 현재 구현에서는 더 짧고 명확한 조건부 update 방식이 적합하다.

#### 대안 4. DB 조건부 업데이트를 최종 판매 확정 기준으로 사용

- 장점
  - `soldQuantity < totalQuantity` 조건을 DB update 문에 포함해 원자적으로 차감할 수 있다.
  - update count로 재고 차감 성공 여부를 명확히 판단할 수 있다.
  - Redis 장애나 Redis 재고 불일치가 있어도 DB 기준으로 초과 판매를 막을 수 있다.
  - 결제 실패나 저장 실패 시 같은 트랜잭션 rollback으로 DB 재고 차감을 취소할 수 있다.
- 단점
  - DB가 최종 병목이 된다.
  - 순간 트래픽을 모두 DB로 보내면 connection과 lock 경합이 커질 수 있다.
  - Redis rate limit, Redis 재고 선점, checkout cache 같은 앞단 완충 장치가 필요하다.
- 판단
  - 최종 재고 정합성 보장 방식으로 가장 적합하다.
  - Redis는 DB 부하를 줄이는 사전 완충 계층으로 두고, 최종 판매 확정은 DB가 담당한다.

### 최종 판단

- 최종 판매 확정은 Redis가 아니라 DB 조건부 업데이트 성공 여부로 판단한다.
- `InventoryRepository.increaseSoldQuantityIfAvailable(productId)`가 다음 작업을 원자적으로 수행한다.
  - `soldQuantity = soldQuantity + 1`
  - `version = version + 1`
  - `where productId = ? and soldQuantity < totalQuantity`
- update count 판단 기준
  - `1`: 재고 차감 성공
  - `0`: 남은 재고 없음, `SOLD_OUT` 처리
- `BookingService.book()`은 하나의 트랜잭션 안에서 다음 작업을 수행한다.
  - DB 재고 차감
  - 결제 승인
  - 예약 저장
  - 결제 승인 내역 저장
  - outbox 이벤트 저장
- 결제 실패, 예약 저장 실패, 중복 예약 실패가 발생하면 트랜잭션 rollback으로 DB 재고 차감도 취소된다.
- Redis 재고 처리 원칙
  - Redis 재고 선점은 빠른 사전 차단이다.
  - Redis 재고 선점 성공은 예약 성공을 의미하지 않는다.
  - DB commit 성공이 최종 예약 성공 기준이다.
- 중복 요청 방어 기준
  - 같은 사용자와 상품의 중복 예약은 `Booking`의 `(userId, productId)` unique 제약으로 방어한다.
  - 같은 멱등성 키의 중복 요청은 `idempotencyKey` unique 제약과 기존 예약 조회로 방어한다.
- 검증 기준
  - `BookingConsistencyTest`에서 Redis 선점 없이 동시에 여러 예약 요청이 들어와도 실제 성공 예약 수와 `soldQuantity`가 총 재고를 초과하지 않음을 확인한다.
- 최종 원칙
  - Redis는 빠른 재고 선점과 트래픽 완충 계층이다.
  - MySQL은 최종 재고 원장이다.
  - 초과 판매 방지는 DB 조건부 업데이트로 보장한다.

## 8. MSA 확장을 고려한 서버 간 정합성 보장 방식

### 쟁점

- 2대 이상의 애플리케이션 서버가 동시에 예약 요청을 처리할 때 서버 간 정합성을 어떻게 보장할 것인가?
- 향후 MSA로 분리될 때 이벤트 발행 및 소비의 정합성을 어떤 구조로 확장할 것인가?
- 핵심 판단 기준
  - 서버 로컬 상태에 의존하지 않고 최종 정합성을 보장할 수 있는가?
  - 재고 초과 판매와 중복 예약을 DB 기준으로 방어할 수 있는가?
  - 예약 확정 이벤트가 유실되지 않도록 발행 측 정합성을 확보할 수 있는가?
  - 향후 consumer 멱등 처리와 서비스 간 보상 트랜잭션을 확장할 여지를 남기는가?

### 문제 상황

- 애플리케이션 서버가 2대 이상이면 서버 로컬 메모리만으로 다음 상태를 공유할 수 없다.
  - 재고 차감 상태
  - 사용자별 중복 예약 상태
  - 멱등성 키 처리 상태
  - 이벤트 발행 상태
- Redis는 여러 서버가 공유할 수 있는 빠른 저장소지만 최종 원장은 아니다.
  - Redis 장애가 발생할 수 있다.
  - Redis 재고와 DB 재고가 일시적으로 불일치할 수 있다.
  - Redis 재고 복구가 실패할 수 있다.
- Kafka 발행이 예약 트랜잭션과 분리되면 다음 문제가 생길 수 있다.
  - 예약은 성공했지만 이벤트 발행이 실패한다.
  - 같은 이벤트가 중복 발행된다.
  - consumer가 같은 이벤트를 중복 처리한다.
- 현재 구현은 단일 애플리케이션 안에서 MSA 확장을 고려한 구조를 갖는다.
  - 서버 간 예약/재고 정합성은 구현되어 있다.
  - Outbox 기반 이벤트 발행 측 정합성은 구현되어 있다.
  - `ProcessedEvent` 기반 consumer 멱등 처리 토대는 있다.
  - 실제 `@KafkaListener` consumer와 서비스 간 보상 트랜잭션은 아직 구현되어 있지 않다.

### 고려했던 대안들

#### 대안 1. 애플리케이션 서버 로컬 상태로 정합성 관리

- 장점
  - 구현이 단순하다.
  - 외부 저장소 접근 없이 빠르게 판단할 수 있다.
- 단점
  - 서버가 2대 이상이면 인스턴스별 상태가 분리된다.
  - 같은 상품의 재고 차감 상태를 일관되게 관리할 수 없다.
  - 사용자별 중복 예약과 멱등성 처리가 서버마다 다르게 판단될 수 있다.
  - 서버 재시작 시 상태가 사라진다.
- 판단
  - 분산 서버 환경 요구에 맞지 않는다.
  - 최종 정합성 보장 방식으로 사용할 수 없다.

#### 대안 2. Redis를 서버 간 최종 정합성 기준으로 사용

- 장점
  - 여러 애플리케이션 서버가 같은 Redis key를 기준으로 빠르게 판단할 수 있다.
  - 재고 선점, rate limit, 멱등성 사전 점유에 적합하다.
  - DB 부하를 줄일 수 있다.
- 단점
  - Redis는 예약, 결제, outbox 저장과 같은 DB 트랜잭션에 함께 묶이지 않는다.
  - Redis 장애 시 최종 판단을 할 수 없다.
  - Redis 재고 복구 실패 또는 키 누락 시 DB와 불일치할 수 있다.
  - 영속 원장과 감사 기준으로는 MySQL보다 약하다.
- 판단
  - Redis는 서버 간 빠른 완충 계층으로 사용한다.
  - 최종 정합성 기준은 MySQL이 담당해야 한다.

#### 대안 3. MySQL 제약과 조건부 업데이트를 서버 간 최종 정합성 기준으로 사용

- 장점
  - 여러 서버가 동시에 요청해도 같은 DB 원장을 기준으로 판단할 수 있다.
  - DB 조건부 업데이트로 초과 판매를 막을 수 있다.
  - unique 제약으로 중복 예약과 멱등성 중복을 최종 방어할 수 있다.
  - 트랜잭션 rollback으로 결제 실패나 저장 실패 시 재고 차감을 되돌릴 수 있다.
- 단점
  - DB가 최종 병목이 된다.
  - 모든 요청이 DB까지 도달하면 lock 경합과 connection 부하가 커진다.
  - Redis rate limit, Redis 재고 선점, checkout cache 같은 앞단 완충 장치가 필요하다.
- 판단
  - 현재 구현의 서버 간 최종 정합성 기준으로 채택한다.
  - 상세한 재고 정합성 기준은 `7. 최종 재고 정합성 보장 방식`을 따른다.

#### 대안 4. Transactional Outbox로 이벤트 발행 정합성 확보

- 장점
  - 예약, 결제, 재고 변경과 이벤트 저장을 같은 DB 트랜잭션에 묶을 수 있다.
  - Kafka 장애 시에도 예약 성공 이벤트를 DB에 남겨 재시도할 수 있다.
  - 발행 실패를 API 성공 여부와 직접 결합하지 않을 수 있다.
- 단점
  - Outbox publisher가 같은 이벤트를 중복 발행할 수 있다.
  - consumer는 반드시 멱등하게 처리해야 한다.
  - 실제 consumer 처리, 재처리, 보상 트랜잭션은 별도 설계가 필요하다.
- 판단
  - 발행 측 정합성 확보 방식으로 채택한다.
  - 상세한 Outbox 기준은 `10. Transactional Outbox 패턴을 도입한 이유`를 따른다.
  - 소비 측 멱등 처리와 서비스 간 보상 트랜잭션은 향후 MSA 확장 과제로 둔다.

### 최종 판단

- 현재 구현 범위에서는 여러 애플리케이션 서버가 같은 MySQL과 Redis를 공유하는 분산 서버 환경을 기준으로 정합성을 보장한다.
- 서버 간 최종 재고 정합성
  - DB 조건부 업데이트를 최종 판매 확정 기준으로 사용한다.
  - 상세 기준은 `7. 최종 재고 정합성 보장 방식`을 따른다.
- 서버 간 중복 예약 방지
  - 같은 사용자와 상품의 중복 예약은 DB unique 제약으로 최종 방어한다.
- 서버 간 멱등성 보장
  - 같은 멱등성 키의 중복 요청은 DB unique 제약과 기존 예약 조회로 방어한다.
- 이벤트 발행 정합성
  - 예약 확정 이벤트는 Outbox에 저장하고 Kafka로 비동기 발행한다.
  - Kafka 도입 기준은 `9. Kafka를 도입한 이유`를 따른다.
  - Outbox 발행 정합성 기준은 `10. Transactional Outbox 패턴을 도입한 이유`를 따른다.
- 향후 MSA 확장 범위
  - 실제 consumer는 아직 구현하지 않는다.
  - `ProcessedEvent`는 향후 consumer 멱등 처리를 위한 토대로 둔다.
  - 서비스 간 보상 트랜잭션, saga, consumer 재처리 정책은 별도 결정 기록으로 다룬다.
- 최종 원칙
  - 현재 구현은 발행 측 정합성과 서버 간 예약/재고 정합성을 우선 보장한다.

## 9. Kafka를 도입한 이유

### 쟁점

- 예약 확정 이후 후속 처리를 Booking API 트랜잭션 안에서 직접 처리할 것인가, 이벤트 기반으로 분리할 것인가?
- 예약 확정 이벤트를 어떤 메시징 시스템으로 전달할 것인가?
- 핵심 판단 기준
  - 예약 확정 API 응답 시간이 후속 처리에 의해 길어지지 않는가?
  - 알림, 통계, 포인트, 외부 연동 장애가 예약 성공 자체를 흔들지 않는가?
  - 순간적인 이벤트 발행량 증가를 감당할 수 있는가?
  - 예약 단위 이벤트 순서 보장과 consumer 확장이 가능한가?
  - 이벤트 보관과 재처리를 지원할 수 있는가?
  - 중복 발행 가능성을 consumer 멱등 처리로 확장할 수 있는가?

### 문제 상황

- 예약 확정 이후에는 여러 후속 처리가 필요할 수 있다.
  - 예약 확정 알림
  - 통계 적재
  - 포인트 서비스 연동
  - 외부 시스템 이벤트 전달
- 이 후속 처리를 Booking API에서 모두 동기 처리하면 다음 문제가 생긴다.
  - Booking API 응답 시간이 길어진다.
  - 후속 서비스 장애가 예약 성공 API에 직접 전파된다.
  - 예약 확정 트랜잭션의 책임이 비대해진다.
- 프로모션 시점에는 예약 성공 이벤트 발행량도 순간적으로 증가할 수 있다.
  - 단순 작업 큐보다 이벤트 스트림 기반 확장성이 필요하다.
  - 여러 consumer가 같은 예약 확정 이벤트를 각자 처리할 수 있어야 한다.
  - 장애 복구나 consumer 재처리를 위해 이벤트 replay 가능성이 중요하다.
- 따라서 Booking API와 후속 처리를 분리하면서도 이벤트 스트림 확장성에 적합한 MQ를 선택해야 한다.

### 고려했던 대안들

#### 대안 1. Booking API에서 후속 처리를 모두 동기 실행

- 장점
  - 흐름이 단순하다.
  - API 응답 시점에 후속 처리 결과까지 확인할 수 있다.
  - 별도 메시지 브로커나 outbox 구조가 필요 없다.
- 단점
  - 후속 처리 시간이 Booking API 응답 시간에 포함된다.
  - 알림, 통계, 외부 연동 장애가 예약 성공 흐름에 직접 영향을 준다.
  - 후속 처리가 늘어날수록 BookingService 책임이 비대해진다.
  - MSA 분리 시 서비스 간 결합도가 높아진다.
- 판단
  - 예약 확정에 필수적이지 않은 후속 처리는 Booking API에서 분리해야 한다.

#### 대안 2. 메시지 큐 도입

- 장점
  - Booking API와 예약 이후 후속 처리를 분리할 수 있다.
  - 알림, 통계, 포인트, 외부 연동 장애가 예약 성공 흐름에 직접 전파되지 않는다.
  - 향후 MSA 분리 시 서비스 간 결합도를 낮출 수 있다.
- 단점
  - 메시지 브로커 운영 부담이 추가된다.
  - 메시지 유실, 중복 발행, 순서 보장, 재처리 기준을 별도로 설계해야 한다.
  - consumer 멱등 처리가 필요하다.
- 판단
  - 예약 확정에 필수적이지 않은 후속 처리는 메시지 큐로 분리한다.
  - 어떤 MQ를 사용할지는 처리량, 순서 보장, replay, consumer 확장성을 기준으로 비교한다.

#### 대안 2-1. RabbitMQ

- 장점
  - 라우팅 기능과 작업 큐 모델이 명확하다.
  - 일반적인 비동기 작업 처리에 적합하다.
  - 메시지 단위 ack와 retry 흐름을 구성하기 쉽다.
- 단점
  - 대량 이벤트 스트림 보관과 replay 관점에서는 Kafka보다 불리하다.
  - consumer group 기반 병렬 처리 모델은 Kafka보다 약하다.
  - 예약 확정 이벤트를 여러 후속 서비스가 독립적으로 재처리하는 구조에는 추가 설계가 필요하다.
- 판단
  - 단일 작업 큐나 명령 전달에는 적합하다.
  - 예약 확정 이벤트 스트림의 장기 확장 기준으로는 Kafka보다 약하다.

#### 대안 2-2. Redis Streams

- 장점
  - 이미 Redis를 사용하므로 인프라 구성이 단순하다.
  - 빠른 메시지 처리에 적합하다.
  - consumer group을 사용할 수 있다.
- 단점
  - Redis 장애 fallback 대상과 이벤트 전달 계층이 겹친다.
  - Redis를 rate limit, cache, 재고 선점 외에 이벤트 전달 책임까지 맡기면 장애 영향 범위가 커진다.
  - 장기 보관, 대규모 consumer 확장, replay 운영 관점에서는 Kafka보다 약하다.
- 판단
  - Redis는 빠른 완충 계층으로 둔다.
  - 예약 확정 이벤트 전달 책임까지 Redis에 확대하지 않는다.

#### 대안 2-3. Kafka

- 장점
  - 높은 처리량의 이벤트 스트림 처리에 적합하다.
  - `aggregateId` 같은 key를 기준으로 같은 예약 이벤트를 같은 partition에 기록할 수 있다.
  - consumer group을 통해 후속 서비스 처리량을 확장할 수 있다.
  - 이벤트 보관과 replay가 가능해 장애 복구와 재처리에 유리하다.
- 단점
  - RabbitMQ나 Redis Streams보다 운영 복잡도가 높다.
  - publisher 재시도 과정에서 중복 발행 가능성이 남는다.
  - consumer는 멱등하게 구현해야 한다.
- 판단
  - 예약 확정 이벤트의 확장성, 순서 보장, 재처리 가능성을 고려해 Kafka를 선택한다.
  - Kafka는 예약 성공 여부를 결정하는 원장이 아니라 outbox 이벤트를 전달하는 비동기 메시징 수단으로 사용한다.

### 최종 판단

- 예약 확정 이후 후속 처리는 Kafka 기반 비동기 이벤트로 분리한다.
- Kafka 선택 이유
  - 예약 확정 이벤트는 알림, 통계, 포인트, 외부 연동 등 여러 consumer로 확장될 수 있다.
  - `aggregateId`를 key로 사용해 같은 예약 이벤트가 같은 Kafka partition에 기록되도록 한다.
  - consumer group을 통해 후속 서비스별 처리량을 확장할 수 있다.
  - 이벤트 보관과 replay가 가능해 장애 복구와 재처리에 유리하다.
  - RabbitMQ, Redis Streams보다 이벤트 스트림 확장성과 재처리 기준에 더 적합하다.
- Kafka는 예약 성공을 결정하는 수단이 아니다.
- Kafka는 예약 확정 이후 이벤트를 전달하는 메시징 수단이다.
- 메시지 유실 방지와 발행 재시도 기준은 `10. Transactional Outbox 패턴을 도입한 이유`에서 다룬다.

## 10. Transactional Outbox 패턴을 도입한 이유

### 쟁점

- 예약 DB commit과 Kafka 발행 사이에서 메시지 유실을 어떻게 막을 것인가?
- Kafka 장애나 애플리케이션 장애가 예약 성공 이벤트 유실로 이어지지 않게 할 것인가?
- Kafka 발행 실패를 어떻게 추적하고 재시도할 것인가?
- 핵심 판단 기준
  - 예약 DB transaction과 이벤트 발행 사이에서 메시지 유실이 발생하지 않는가?
  - Kafka 장애 시 이벤트를 재시도할 수 있는가?
  - Kafka 장애가 예약 성공 여부를 직접 흔들지 않는가?
  - 중복 발행 가능성을 consumer 멱등 처리로 확장할 수 있는가?

### 문제 상황

- Kafka를 도입하더라도 DB commit과 Kafka 발행은 하나의 transaction으로 묶이지 않는다.
- DB commit 이후 Kafka에 직접 발행하면 다음 문제가 생긴다.
  - DB commit은 성공했지만 Kafka 발행 전에 서버가 죽으면 이벤트가 유실된다.
  - Kafka 장애 시 예약은 성공했지만 외부 시스템은 예약 확정을 알 수 없다.
  - 발행 실패 이벤트를 안정적으로 추적하기 어렵다.
- Kafka 발행 후 DB commit을 수행하면 다음 문제가 생긴다.
  - Kafka 발행은 성공했지만 DB commit이 실패할 수 있다.
  - 실제 예약이 없는 예약 확정 이벤트가 외부로 전달될 수 있다.
  - 예약 원장과 이벤트 간 정합성이 깨진다.
- 따라서 예약 원장과 이벤트 발행 요청을 같은 트랜잭션 안에 남기고, Kafka 발행은 별도 재시도 흐름으로 분리해야 한다.

### 고려했던 대안들

#### 대안 1. DB commit 이후 Kafka 직접 발행

- 장점
  - 구현이 단순하다.
  - 별도 outbox 테이블과 publisher가 필요 없다.
  - 예약 성공 후 바로 이벤트 발행을 시도할 수 있다.
- 단점
  - DB commit 성공 후 Kafka 발행 전에 서버가 죽으면 이벤트가 유실된다.
  - Kafka 장애 시 발행 실패 이벤트를 안정적으로 추적하기 어렵다.
  - 재시도 기준을 별도 저장소 없이 관리하기 어렵다.
- 판단
  - 메시지 유실 방지 요구에 맞지 않는다.
  - 예약 원장과 이벤트 발행 요청을 함께 남길 수 있는 구조가 필요하다.

#### 대안 2. Kafka 발행 후 DB commit

- 장점
  - Kafka 발행 성공을 먼저 확인할 수 있다.
  - 외부 시스템에 이벤트가 빠르게 전달될 수 있다.
- 단점
  - Kafka 발행은 성공했지만 DB commit이 실패할 수 있다.
  - 실제 존재하지 않는 예약 확정 이벤트가 발행될 수 있다.
  - 예약 원장과 이벤트 간 정합성이 깨진다.
- 판단
  - 예약 시스템에서는 DB 예약 원장이 우선이다.
  - 실제 예약 확정 전 이벤트가 외부로 나가는 구조는 적합하지 않다.

#### 대안 3. Transactional Outbox

- 장점
  - 예약, 결제, 재고 변경과 outbox 이벤트 저장을 같은 DB 트랜잭션에 묶을 수 있다.
  - DB commit이 성공하면 이벤트 발행 요청도 함께 남는다.
  - Kafka 장애 시 outbox 이벤트를 PENDING 상태로 남겨 재시도할 수 있다.
  - Kafka 장애가 Booking API 성공 여부에 직접 전파되지 않는다.
  - 향후 consumer 멱등 처리와 조합해 MSA 이벤트 처리로 확장할 수 있다.
- 단점
  - outbox 테이블과 publisher가 필요하다.
  - 재시도 상태와 최대 재시도 횟수를 관리해야 한다.
  - publisher 장애나 재시도 과정에서 중복 발행 가능성이 남는다.
  - consumer는 반드시 멱등하게 처리해야 한다.
- 판단
  - 예약 확정 이벤트 유실 방지 요구에 가장 적합하다.
  - 현재 구현에서는 발행 측 정합성을 보장하고, 소비 측 멱등 처리는 향후 MSA 확장 과제로 둔다.

### 최종 판단

- Booking API는 Kafka에 직접 발행하지 않는다.
- 예약 확정 트랜잭션 처리
  - `BookingService`는 예약, 결제, 재고 변경과 같은 DB 트랜잭션 안에서 `OutboxEvent.pending(...)`을 저장한다.
  - outbox 이벤트 저장은 예약 확정과 같은 transaction에 포함된다.
  - 결제 실패나 예약 저장 실패 시 outbox 이벤트도 저장되지 않는다.
- Kafka 발행 처리
  - `OutboxPublisher`는 PENDING 이벤트를 조회해 `MessagePublisher`로 발행한다.
  - `KafkaMessagePublisher`는 Kafka topic으로 이벤트 payload를 발행한다.
  - `aggregateId`를 key로 사용해 같은 예약 이벤트가 같은 Kafka partition에 기록되도록 한다.
- 발행 성공 기준
  - Kafka 발행이 성공하면 outbox 이벤트 상태를 `PUBLISHED`로 변경한다.
  - `publishedAt`을 기록해 재발행 대상에서 제외한다.
- 발행 실패 기준
  - Kafka 발행 실패 시 `retryCount`를 증가시킨다.
  - `nextRetryAt`을 설정하고 PENDING 상태로 재시도 대상에 남긴다.
  - 최대 재시도 횟수를 넘으면 `FAILED`로 전환한다.
- 현재 테스트 범위
  - Outbox 발행 성공/실패 테스트는 `11. 정합성 및 동시성 테스트 전략`에서 다룬다.
- 향후 확장 기준
  - Outbox publisher는 중복 발행 가능성이 있으므로 consumer는 멱등하게 처리해야 한다.
  - 실제 Kafka consumer는 아직 구현하지 않는다.
  - `ProcessedEvent`는 향후 consumer 멱등 처리를 위한 토대로 둔다.
  - consumer 멱등 처리 테스트 기준은 `11. 정합성 및 동시성 테스트 전략`에서 다룬다.
  - consumer 멱등 처리, 보상 트랜잭션, saga는 별도 결정 기록으로 다룬다.
- 최종 원칙
  - Kafka는 예약 성공을 결정하는 수단이 아니다.
  - MySQL outbox가 이벤트 발행 요청의 원장이다.
  - Kafka 장애는 예약 성공을 되돌리지 않고 outbox 재시도로 복구한다.

## 11. 정합성 및 동시성 테스트 전략

### 쟁점

- 서버 간 정합성 보장 방식이 실제 테스트로 검증되는가?
- 동시 예약, 중복 예약, Outbox 재시도, Redis fallback을 어떤 수준까지 검증할 것인가?
- MSA consumer 멱등 처리는 현재 구현 범위인지 향후 범위인지 어떻게 구분할 것인가?
- 핵심 판단 기준
  - 초과 판매와 중복 예약 방어가 동시성 상황에서 검증되는가?
  - Redis 장애 또는 키 누락 시 fallback 흐름이 검증되는가?
  - Outbox 발행 성공/실패와 재시도 상태 전이가 검증되는가?
  - 아직 구현되지 않은 MSA consumer 흐름을 검증 완료 범위로 쓰지 않는가?

### 문제 상황

- 분산 서버 환경의 핵심 위험은 단위 테스트만으로 충분히 검증하기 어렵다.
  - 여러 요청이 동시에 같은 상품 재고를 차감할 수 있다.
  - 같은 사용자가 같은 상품을 동시에 예약할 수 있다.
  - Redis 장애 또는 키 누락 시 DB 기준 fallback이 필요할 수 있다.
  - Kafka 발행 실패 시 Outbox 재시도 상태가 남아야 한다.
- 현재 구현은 실제 MSA consumer까지 포함하지 않는다.
  - `ProcessedEvent`는 consumer 멱등 처리를 위한 도메인 토대다.
  - 실제 `@KafkaListener` consumer는 아직 구현되어 있지 않다.
  - 서비스 간 보상 트랜잭션과 saga도 아직 구현되어 있지 않다.
- 따라서 현재 구현된 범위와 향후 검증할 범위를 문서에서 분리해야 한다.

### 고려했던 대안들

#### 대안 1. 단위 테스트 중심 검증

- 장점
  - 실행 속도가 빠르다.
  - 도메인 정책과 예외 케이스를 좁은 범위에서 검증하기 쉽다.
  - 외부 인프라 의존이 적다.
- 단점
  - 동시 요청 상황의 초과 판매와 중복 예약 방어를 충분히 검증하기 어렵다.
  - DB unique 제약과 조건부 업데이트가 실제로 함께 동작하는지 확인하기 어렵다.
  - Outbox publisher의 재시도 흐름은 별도 검증이 필요하다.
- 판단
  - 단위 테스트는 기본 검증으로 유지한다.
  - 서버 간 정합성 요구를 검증하는 유일한 수단으로는 부족하다.

#### 대안 2. 정합성/동시성 테스트를 별도 작성

- 장점
  - 동시 예약 요청에서도 초과 판매가 발생하지 않는지 검증할 수 있다.
  - 같은 사용자의 중복 예약 요청이 하나만 성공하는지 검증할 수 있다.
  - Redis fallback과 Outbox 재시도 흐름을 구현 범위 안에서 확인할 수 있다.
- 단점
  - 단위 테스트보다 실행 비용이 크다.
  - 동시성 테스트는 타이밍에 따라 재현성이 흔들릴 수 있다.
  - 외부 인프라는 mock 또는 대체 구현을 사용해야 한다.
- 판단
  - 현재 프로젝트의 핵심 위험을 검증하는 방식으로 채택한다.
  - 구현된 범위의 정합성, 동시성, 장애 fallback을 우선 검증한다.

#### 대안 3. 실제 MSA consumer까지 포함한 end-to-end 테스트

- 장점
  - Kafka 발행부터 consumer 처리까지 실제 서비스 간 흐름을 검증할 수 있다.
  - consumer 멱등 처리와 보상 트랜잭션까지 검증 범위에 포함할 수 있다.
- 단점
  - 현재 실제 Kafka consumer와 서비스 간 보상 트랜잭션이 구현되어 있지 않다.
  - 아직 구현되지 않은 흐름을 테스트 완료 범위로 쓰면 문서와 코드가 불일치한다.
  - 테스트 인프라와 실행 비용이 크게 증가한다.
- 판단
  - 현재 범위에서는 채택하지 않는다.
  - MSA consumer가 추가될 때 별도 테스트 전략으로 확장한다.

### 최종 판단

- 현재는 구현된 범위에 맞춰 정합성/동시성 테스트와 Outbox publisher 테스트를 우선한다.
- 현재 테스트 범위
  - `BookingConsistencyTest.concurrentBookingsDoNotOversellSingleStock`
    - Redis 선점 없이 동시 예약 요청이 들어와도 성공 예약 수와 `soldQuantity`가 총 재고를 초과하지 않음을 검증한다.
  - `BookingConsistencyTest.concurrentSameUserBookingsCreateOnlyOneBooking`
    - 같은 사용자의 동시 중복 예약 요청 중 하나만 성공함을 검증한다.
  - `OutboxPublisherTest`
    - 이벤트 발행 성공/실패에 따른 outbox 상태 변경과 재시도 예약을 검증한다.
  - `RedisInventoryStockGateTest`, `CheckoutProductCacheTest`
    - Redis 장애 또는 키 누락 시 fallback 흐름을 검증한다.
  - `ProcessedEventTest`
    - consumer 멱등 처리에 사용할 `ProcessedEvent` 생성 기준을 검증한다.
- 향후 테스트 범위
  - 실제 `@KafkaListener` consumer는 아직 테스트하지 않는다.
  - MSA consumer가 추가되면 `ProcessedEvent` 저장을 기준으로 중복 이벤트 무시 테스트를 작성한다.
  - 서비스 간 보상 트랜잭션, saga, consumer 재처리 정책이 구현되면 별도 통합 테스트를 작성한다.
- 최종 원칙
  - 테스트 문서는 현재 구현된 범위와 향후 검증 범위를 분리해서 쓴다.
  - 구현되지 않은 MSA consumer 흐름은 검증 완료로 표현하지 않는다.

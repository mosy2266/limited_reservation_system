# Repository Guidelines

## 개요
해당 레포지토리에서는 특정 시간(00시)에 오픈되는 '초특가 숙소 상품(10개 한정)'에 대한 선착순 예약
시스템을 구축한다.

2대 이상의 애플리케이션 서버로 구성된 분산 환경을 가정하며, 다음의 요건을 충족해야 한다.
- 모든 사용자에게 동등한 기회 제공
- 엄격한 재고 정합성 유지
- 결제 실패 및 시스템 장애 상황에서도 안정적인 동작 보장

주문서 진입 API와 결제 및 예약 완료 API 2개의 필수 API를 구현해야 한다.

## 기본 원칙
- 모든 작업은 단계적으로 진행한다.
- 커밋은 기능 단위가 아닌 '의미 단위'로 진행한다. (메소드 하나의 변경이라도 의미가 명확하게 분리된다면 커밋 타이밍으로 판단한다.)
  - 실제 커밋은 사용자가 직접 진행하며, 적절한 한국어 커밋 메시지 예시를 제공한다.
  - 테스트가 통과하지 않는 경우도 커밋은 하되, WIP 등의 태그로 명시한다.
- 요청 범위를 벗어나는 리팩토링은 하지 않는다.
- 기존 동작을 바꾸는 수정은 최소 범위로 진행한다.
- 사용자가 요청하지 않은 의존성 추가는 피한다.
  - 라이브러리, 의존성, 미들웨어 등의 추가가 필요할 때는 사용자와 필수적으로 논의를 거친 뒤 결정한다.
  - 이 논의에서는 다른 가능한 선택지들, 선택지들 간의 트레이드오프 비교, 기존 구조와의 충돌 가능성, 확장성 등을 고려한다.
- 패키지 구조를 임의로 재편하지 않는다.
- 변경 전 관련 파일과 영향 범위를 먼저 파악한 뒤 수정한다.
- AI 처리 과정 메시지와 응답 메시지는 모두 한국어로 작성한다.
- 응답에 한글 인코딩 깨짐 또는 타 언어 혼입이 보이면 즉시 한국어 문장으로 정정한다.
    - 응답 생성 시 한글(UTF-8) 인코딩이 깨지지 않도록 확인하고, 깨진 문자열(예: `��`) 또는 타 언어 문장이 섞이면 즉시 전체 문장을 한국어로 다시 작성한다.
- 한국어로 주석을 작성하고 내용이 바뀌면 주석도 변경한다.
- 공통 응답/예외 구조를 사용한다.
- 이해하지 못한 요구 사항이 있으면 반드시 사용자에게 되물어본다.
- 보안에 취약한 코드는 특별한 지시가 없다면 사용 금지한다.
- 추측성 수정은 절대 금지한다. 원인을 모르면 절대 수정하지 않는다.
- 간단하지 않은 문제라면 웹 검색을 통해 최신 해결책을 검색한다.
- 문제 해결을 위한 임시 코드 작성을 금지한다.
- 코드 작업 시 `docs/` 폴더의 모든 Markdown 문서를 숙지하고, 해당 문서에 정의된 규칙을 준수한다. `docs` 폴더가 없을 경우 이 지침은 무시해도 된다.

## 기본 구조
다음 계층 구조를 사용한다.

Controller -> Service -> Domain -> Repository

규칙:
- Controller에는 비즈니스 로직을 작성하지 않는다.
- Service는 유스케이스 흐름만 조합한다.
- 핵심 정책과 검증은 Domain이 담당한다.
- Repository는 영속성 처리만 담당한다.

## 분산 환경 고려 및 MSA 친화적 구조
시스템은 최소 2대 이상의 애플리케이션 서버 환경을 가정한다.

완전한 MSA 구현은 아니더라도,
향후 서비스 분리가 가능하도록 패키지 및 계층을 구성한다.

예시:
- booking
- payment
- inventory
- product

도메인 단위로 패키지를 분리하며,
강한 결합(Tight Coupling)을 지양한다.

## 테스트 코드
테스트 코드는 필수로 작성한다.

- 단위 테스트를 우선적으로 작성한 뒤, 성공 케이스/실패(예외) 케이스/경계값 케이스 작성이 모두 끝나면 중요도가 높은 순서대로 통합 테스트를 작성한다.
- MSA 환경에서 필요한 테스트(인스턴스 간 정합성 등) 및 정합성, 동시성 관련 테스트를 가장 높은 우선순위로 작성한다.
- Redis, Kafka 등 미들웨어가 존재할 경우 외부 mock을 통한 테스트를 고려한다.

## 부칙
- 실제 PG사와의 연동은 생략하되, 인터페이스 등을 통해 구조적으로 흐름이 이어지도록 구현한다.
- 로컬 프로젝트이므로 회원 인증 및 로그인 보안 처리는 구현 범위에서 제외한다.
- 인프라 증설(Scale-up/out)이 제한적인 상황을 가정한다.
- 평시 50TPS이며, 프로모션이 시작되는 00시에 약 1~5분간 순간적으로 500~1000TPS 가 예상된다.
- 모든 사용자가 동등한 확률로 상품을 구매할 수 있는 구조를 고민해야 한다.
- 결제 수단으로는 신용카드, Y페이, Y포인트를 지원한다.
- 복합 결제는 (신용카드 + 포인트) 또는 (Y페이 + 포인트) 가능. 단, 신용카드와 Y페이는 혼용 불가하다.
